import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the floating dev-tools badge — it sits over the composer in this layout.
  devIndicators: false,
};

export default nextConfig;
